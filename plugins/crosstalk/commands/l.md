---
description: /crosstalk:launch 의 짧은 별칭. cmux 자동 실행 + 분할 + AI CLI 시작 + 라벨링.
allowed-tools: Bash, AskUserQuestion
argument-hint: (인자 없음)
---

# /crosstalk:l (별칭)

이 명령은 `/crosstalk:launch` 와 동일하게 동작한다.

`/crosstalk:launch` 슬래시 커맨드의 본문(파일: `commands/launch.md`)을 그대로 따라 실행해라.
