---
description: /crosstalk:setup 의 짧은 별칭. cmux 라벨링.
allowed-tools: Bash, AskUserQuestion
argument-hint: (인자 없음)
---

# /crosstalk:s (별칭)

이 명령은 `/crosstalk:setup` 과 동일하게 동작한다.

`/crosstalk:setup` 슬래시 커맨드의 본문(파일: `commands/setup.md`)을 그대로 따라 실행해라.
