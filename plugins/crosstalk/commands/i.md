---
description: /crosstalk:install 의 짧은 별칭. Crosstalk 셋업 + AI CLI 자동 설치.
allowed-tools: Bash, AskUserQuestion, Read
argument-hint: (인자 없음)
---

# /crosstalk:i (별칭)

이 명령은 `/crosstalk:install` 과 동일하게 동작한다.

`/crosstalk:install` 슬래시 커맨드의 본문(파일: `commands/install.md`)을 그대로 따라 실행해라.
