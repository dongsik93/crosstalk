---
description: /crosstalk:debate 의 짧은 별칭. cmux의 다른 AI들과 자동 토론.
allowed-tools: Bash, AskUserQuestion
argument-hint: <토론 주제>
---

# /crosstalk:d (별칭)

이 명령은 `/crosstalk:debate` 와 동일하게 동작한다.

`/crosstalk:debate <주제>` 슬래시 커맨드의 본문(파일: `commands/debate.md`)을 그대로 따라 실행해라. 인자(`$ARGUMENTS`)는 토론 주제로 사용.
